{
  "name": "jeu-simple-html",
  "private": true,
  "version": "1.0.0",
  "description": "Guerre Spatiale - build helpers",
  "scripts": {
    "build:legacy": "node scripts/build-legacy.mjs",
    "check:legacy": "npm run build:legacy && git diff --exit-code -- js/main.legacy.js",
    "build": "npm run build:legacy"
  }
}
